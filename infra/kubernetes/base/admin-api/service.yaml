apiVersion: v1
kind: Service
metadata:
  name: mys-admin-api
  labels:
    app: mys-admin-api
    component: api
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
  selector:
    app: mys-admin-api

apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: mystira-chain
  labels:
    app: mystira-chain
    component: blockchain
spec:
  serviceName: mystira-chain
  replicas: 3
  selector:
    matchLabels:
      app: mystira-chain
  template:
    metadata:
      labels:
        app: mystira-chain
        component: blockchain
        azure.workload.identity/use: "true"
    spec:
      serviceAccountName: mystira-chain-sa
      securityContext:
        fsGroup: 1000
        runAsUser: 1000
        runAsNonRoot: true
      nodeSelector:
        workload: chain
      tolerations:
        - key: "workload"
          operator: "Equal"
          value: "chain"
          effect: "NoSchedule"
      containers:
        - name: chain
          image: mystiraacr.azurecr.io/chain:latest
          imagePullPolicy: Always
          ports:
            - name: rpc
              containerPort: 8545
              protocol: TCP
            - name: ws
              containerPort: 8546
              protocol: TCP
            - name: p2p-tcp
              containerPort: 30303
              protocol: TCP
            - name: p2p-udp
              containerPort: 30303
              protocol: UDP
          env:
            - name: CHAIN_DATA_DIR
              value: /data
            - name: CHAIN_NETWORK_ID
              valueFrom:
                configMapKeyRef:
                  name: mystira-chain-config
                  key: network_id
            - name: KEY_VAULT_URL
              valueFrom:
                configMapKeyRef:
                  name: mystira-chain-config
                  key: key_vault_url
            - name: AZURE_CLIENT_ID
              valueFrom:
                secretKeyRef:
                  name: mystira-chain-secrets
                  key: azure_client_id
                  optional: true
          resources:
            requests:
              memory: "2Gi"
              cpu: "500m"
            limits:
              memory: "4Gi"
              cpu: "2000m"
          volumeMounts:
            - name: chain-data
              mountPath: /data
          livenessProbe:
            httpGet:
              path: /health
              port: 8545
            initialDelaySeconds: 60
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /ready
              port: 8545
            initialDelaySeconds: 30
            periodSeconds: 10
  volumeClaimTemplates:
    - metadata:
        name: chain-data
      spec:
        accessModes: ["ReadWriteOnce"]
        storageClassName: managed-premium
        resources:
          requests:
            storage: 100Gi
---
apiVersion: v1
kind: Service
metadata:
  name: mystira-chain
  labels:
    app: mystira-chain
spec:
  type: ClusterIP
  ports:
    - name: rpc
      port: 8545
      targetPort: 8545
    - name: ws
      port: 8546
      targetPort: 8546
  selector:
    app: mystira-chain
---
apiVersion: v1
kind: Service
metadata:
  name: mystira-chain-p2p
  labels:
    app: mystira-chain
  annotations:
    service.beta.kubernetes.io/azure-load-balancer-internal: "true"
spec:
  type: LoadBalancer
  ports:
    - name: p2p-tcp
      port: 30303
      targetPort: 30303
      protocol: TCP
    - name: p2p-udp
      port: 30303
      targetPort: 30303
      protocol: UDP
  selector:
    app: mystira-chain
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: mystira-chain-config
data:
  network_id: "1337"
  chain_id: "1337"
  rpc_enabled: "true"
  ws_enabled: "true"
  key_vault_url: "https://mystira-chain-kv.vault.azure.net/"
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: mystira-chain-sa
  annotations:
    azure.workload.identity/client-id: "${AZURE_CLIENT_ID}"

import { OpenRolesBrowser } from '@/features/Contributor/components/OpenRolesBrowser';

export function OpenRolesPage() {
  return (
    <div className="page page--open-roles">
      <OpenRolesBrowser />
    </div>
  );
}


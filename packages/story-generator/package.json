{
  "name": "@mystira/story-generator",
  "version": "0.1.0",
  "private": true,
  "description": "AI-powered story generation engine for Mystira",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc || true",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "test": "jest",
    "test:unit": "jest --testPathPattern=unit",
    "test:integration": "jest --testPathPattern=integration",
    "lint": "eslint . --ext .ts || true",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.71.2",
    "openai": "^4.20.0"
  },
  "devDependencies": {
    "@types/node": "^25.0.1",
    "tsx": "^4.7.0",
    "typescript": "^5.3.0"
  }
}

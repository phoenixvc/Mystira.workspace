{
  "name": "@mystira/chain",
  "version": "0.5.2-alpha",
  "private": true,
  "description": "Blockchain integration service for Story Protocol IP registration and royalties",
  "scripts": {
    "build": "echo 'Python build handled externally'",
    "test": "python -m pytest || echo 'pytest not available, skipping Python tests'",
    "lint": "ruff check .",
    "dev": "python -m mystira_chain"
  }
}

# Mystira Admin UI - Environment Variables Template
# Copy this file to .env.local and fill in your values

# =============================================================================
# API Configuration
# =============================================================================

# Backend API base URL
# Development: http://localhost:5000
# Staging: https://api-staging.mystira.com
# Production: https://api.mystira.com
VITE_API_BASE_URL=http://localhost:5000

# =============================================================================
# Azure AD / Entra ID (MSAL) Configuration
# =============================================================================

# Azure AD Application (Client) ID
# Get this from Azure Portal > App Registrations > Your App > Overview
VITE_AZURE_CLIENT_ID=your-client-id-here

# Azure AD Tenant ID
# Get this from Azure Portal > App Registrations > Your App > Overview
# Use "common" for multi-tenant apps, or your specific tenant ID
VITE_AZURE_TENANT_ID=your-tenant-id-here

# Redirect URI after authentication
# Must match one of the redirect URIs configured in Azure AD App Registration
# Development: http://localhost:7001
# Production: https://admin.mystira.com
VITE_AZURE_REDIRECT_URI=http://localhost:7001

# API Scope for backend API access
# Format: api://{backend-app-client-id}/access_as_user
# Or: api://{backend-app-client-id}/.default
VITE_AZURE_API_SCOPE=api://your-backend-app-id/access_as_user

# Bypass Azure AD authentication (development only)
# Set to "true" to skip Azure AD authentication during development
# WARNING: Only use this in local development, never in production
# VITE_BYPASS_AUTH=true


# =============================================================================
# Application Configuration
# =============================================================================

# Environment identifier (development, staging, production)
VITE_ENV=development

# Enable debug logging (true/false)
VITE_DEBUG=true

# =============================================================================
# Optional Configuration
# =============================================================================

# Application version (auto-generated from package.json if not set)
# VITE_APP_VERSION=0.1.0

# Application name
# VITE_APP_NAME=Mystira Admin UI

# =============================================================================
# Notes
# =============================================================================

# - All VITE_ prefixed variables are exposed to the client-side code
# - Never commit .env.local to version control
# - Keep sensitive values in .env.local only
# - Update this template when adding new environment variables
#
# Azure AD Setup Requirements:
# 1. Register an app in Azure Portal > App Registrations
# 2. Add a Single-Page Application (SPA) redirect URI
# 3. Enable ID tokens and Access tokens in Authentication settings
# 4. Add API permissions for your backend API
# 5. Expose an API with scopes if you need backend access
